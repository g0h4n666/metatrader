[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Puerto interno de la API Flask
# NO exponer públicamente - usar red interna Railway
# metatrader.railway.internal:8080
